services:
  - type: web
    name: cloud
    env: static
    buildCommand: sed -i "s/MAPBOX_TOKEN_HERE/$MAPBOX_TOKEN/g" ubicacion.html
    staticPublishPath: ./
    envVars:
      - key: MAPBOX_TOKEN
        sync: false
