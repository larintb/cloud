services:
  - type: web
    name: cloud
    env: static
    buildCommand: sed -i "s|<script src=\"config.js\"></script>|<script>const MAPBOX_TOKEN = '$MAPBOX_TOKEN';</script>|" ubicacion.html
    staticPublishPath: ./
    envVars:
      - key: MAPBOX_TOKEN
        sync: false
